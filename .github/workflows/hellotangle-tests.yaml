name: hellotangle-tests

on: push

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: NodeJS / NPM Installation
        uses: actions/setup-node@v1
        with:
          node-version: '15'
      - run: npm install

      - name: Tests
        if: ${{ success() }}
        run: cd api && npm test
